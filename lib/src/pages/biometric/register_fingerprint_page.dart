import 'package:flutter/material.dart';

class RegisterFingerprintPage extends StatelessWidget {
  const RegisterFingerprintPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('Register Fingerprint Page'));
  }
}
